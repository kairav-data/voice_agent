faster-whisper>=1.1.0
sounddevice>=0.5.0
numpy>=1.26
requests>=2.32

# text to speech
piper-tts>=1.3          # offline neural voices (default)
edge-tts>=7.0           # online neural voices, most natural
soundfile>=0.12         # mp3 decoding for edge-tts
comtypes>=1.2 ; sys_platform == "win32"   # SAPI5 fallback
